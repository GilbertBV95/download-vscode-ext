extends layout

block content
     p Descarguemos vsix
     form(action="/getExtension", method="POST")
      label(for="url") Url de la extension en marketplace
      input#url(type="text", name="url", value="https://marketplace.visualstudio.com/items?itemName=mongodb.mongodb-vscode")
      br
      label(for="version") Version de la extension
      input#version(type="text", name="version", value="1.12.0")
      button(type="submit") Descargar